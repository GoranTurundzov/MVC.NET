@model List<OrderDetailsViewModel>

@*<div class="row">
        <div class=" col-md-5">
            <h1 class="text-center">Order Details</h1>
        </div>
    </div>
    <h2>User Details</h2>
    <div class=" row">
        <div class="col-md-7">
            <h3>Order Name: @Model.UserFullName</h3>

        </div>
    </div>
    <div class=" row">
        <div class="col-md-7">
            <h1>Pizza Detail</h1>
        </div>
    </div>
    <div class=" row">
        <div class="col-md-7">
            <h3>Pizza: @Model.PizzaName</h3>
            <h4>Price: @Model.Price</h4>
            <p>Payment Method: @Model.PaymentMethod</p>
        </div>*@
@*</div>*@


<div class="col-md-5"></div>
<div class="row">
    <div class="col-md-6">
        @foreach (var item in Model)
        {

            <partial name="~/Views/Shared/Partial/_OrderDetail.cshtml" for="@item" />
            <a class="nav-link" asp-controller="Order" asp-action="OrderDetails" asp-route-id="@item.Id" > Details </a>

        }

        @*@for (var i = 0; i < Model.Count; i++)
        {
            <a class="nav-link" asp-controller="Order" asp-action="OrderDetails" asp-route-id="@Model[i].Id" onclick="Indexot=@Model[i].Id">
                <h3>Order Number @(i+1)  </h3>
                <p>Pizza name: @Model[i].PizzaName</p>
                <p>User : @Model[i].UserFullName</p>
                <p>Payment @Model[i].PaymentMethod</p>
                <p>Delivered: @(Model[i].Delivered ? "Delivered" : "Not Delivered" )</p>
            </a>
        }*@
    </div>
</div>
</div>
<div class="col-md-5">
    
</div>
